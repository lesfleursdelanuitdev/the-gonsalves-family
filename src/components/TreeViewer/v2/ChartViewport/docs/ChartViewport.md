# ChartViewport.tsx

Main viewport for the v2 family tree chart. Renders SVG canvas, grid, ChartContent, optional minimap, right menu, bottom bar. Composes transform and pointer handlers from parent. Local state minimapOpen. Used once in FamilyTree.tsx v2.
