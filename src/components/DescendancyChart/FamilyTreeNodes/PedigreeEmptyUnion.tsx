"use client";

import type { UnionRowProps } from "./UnionNodeView";

/** Pedigree renders persons via flattened {@link TreeNodes}; unions are structural only. */
export function PedigreeEmptyUnion(_props: UnionRowProps): null {
  return null;
}
