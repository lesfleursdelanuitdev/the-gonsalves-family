/**
 * Re-export descendancy layout (layout lives in strategies/descendancy).
 * Kept for tests that import from testdata/layout.
 */

export { layout, markUnions, isContainer } from "../strategies/descendancy";
