/**
 * /tree — reserved for future use (e.g. tree list or landing). Does not redirect.
 */
export default function TreePage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-bg font-body text-muted">
      <p className="text-center">Tree — coming soon.</p>
    </div>
  );
}
